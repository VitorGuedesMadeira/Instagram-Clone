import React from 'react';
import './Reels.css';

const Reels = () => {
  const reels = 'reels';
  return (
    <div className="reels-section">
      {reels}
    </div>
  );
};

export default Reels;
